# 모션 인식 퀴즈 웹앱 제작 계획서

> 국어 수업시간 활용 목적 | 참고 레퍼런스: 인스타그램 AR 필터형 "고개 기울여 정답 선택" 퀴즈

---

## 0. 프로젝트 개요

| 항목 | 내용 |
|---|---|
| 프로젝트명 | 모션 인식 퀴즈 (가칭) |
| 목적 | 국어 수업시간에 활용할, 손을 쓰지 않고 **몸의 동작(모션)만으로 정답을 선택**하는 실시간 퀴즈 게임 |
| DB | Google Sheets (관리자 설정 시트, 문제 시트) |
| 백엔드 | Google Apps Script (`Code.gs`) — Sheets 바운드 스크립트, 웹앱으로 배포 |
| 프론트엔드 | GitHub 저장소 → GitHub Pages 배포 (HTML/CSS/JS) |
| 핵심 차별점 | 클릭이 아닌 **얼굴·손 동작 인식**으로 선택지를 고르는 UX |

### 0-1. 전제 및 가정 (구현 전 확인 필요)

요청 내용에 명시되지 않아, 아래는 계획 수립을 위해 임의로 설정한 **가정**입니다. 실제와 다르면 알려주시면 구조를 조정하겠습니다.

1. **플레이 방식**: 교실 앞 PC(또는 교사용 노트북)에 연결된 웹캠 1대 앞에서 학생이 한 명씩 순서대로 플레이하는 "발표형" 방식으로 가정. (개인 기기로 동시에 여러 명이 플레이하는 방식이면 랭킹보드의 동시성 처리 로직이 추가로 필요합니다.)
2. **모션 인식 처리 위치**: 서버가 아닌 **브라우저(클라이언트) 단에서 실시간 처리**하고, GAS에는 결과 데이터(점수, 정답 여부)만 전송하는 구조로 가정. (GAS는 영상 스트림을 처리할 수 없음 — 기술적 제약)
3. **선택지 개수**: 첨부 화면은 2지선다 예시이므로, 국어 문제 특성상 흔한 **4지선다까지 지원**하는 구조로 확장 설계했습니다.
4. **관리자 인증**: 별도 로그인 시스템 없이 **비밀번호 확인 방식**으로 우선 설계했습니다.

---

## 1. 기능분석

### 1-1. 관리자 기능

| 기능 | 설명 |
|---|---|
| 문제 등록 | 문제 유형(2지/4지선다), 지문, 선택지, 정답, 난이도, 카테고리(문법/어휘/작품이해 등) 입력 → 문제 시트에 저장 |
| 문제 수정/삭제 | 시트 행 단위 수정·삭제 (문제ID 기준) |
| 문제 목록 미리보기 | 등록된 문제를 표 형태로 조회, 필터링(카테고리·난이도) |
| 게임 설정 | 출제 문항 수, 문항당 제한시간, 모션 인식 모드(고개 기울임/손가락 인식), 애니메이션 On/Off 등을 관리자 설정 시트에 저장 |
| 관리자 인증 | 단순 비밀번호 검증 (관리자 설정 시트에 저장된 값과 대조) |

### 1-2. 게임 진행 기능 — 모션 인식 메커니즘 (핵심)

**모드 A. 고개 기울임 (2지선다 — 첨부 화면 방식 재현)**
- 웹캠으로 얼굴을 인식해 양쪽 눈의 좌표를 잇는 선의 기울기(roll angle)를 계산
- 머리를 왼쪽/오른쪽으로 일정 각도 이상 기울이면 해당 방향 선택지가 실시간으로 하이라이트(첨부 화면의 검정/빨강 버블처럼 색상 전환)
- 일정 시간(예: 0.5~0.6초) 이상 각도가 유지되면 해당 선택지로 **자동 확정**
- 화면 상단에는 첨부 화면처럼 **회전된 칠판형 문제 박스 + 원형 제한시간 게이지**를 배치

**모드 B. 손가락 개수 인식 (3~4지선다 확장 — 창의적 확장 기능)**
- 손을 들어 펼친 손가락 개수(1~4개)로 선택지 번호를 지정
- 화면에 실시간 인식된 숫자를 표시해 학생이 자신의 손동작이 올바르게 인식되고 있는지 확인 가능
- 짧게 정지 유지 시 확정, 인식 실패 시 안내 문구 표시

**공통 진행 로직**
1. 게임 시작 → **1회의 API 호출(`getGameData`)로 설정값 + 전체 문제 목록을 일괄 프리로드**한 뒤 문항 수만큼 **중복 없이 랜덤 추출**해 클라이언트 메모리에 적재 (문항 전환 시 서버 재호출 없음 — 2-1절)
2. 문항별 제한시간 카운트다운(원형 progress bar)
3. 제한시간 내 미확정 시 자동 오답 처리 후 다음 문제로 전환
4. 정답/오답에 따라 화면 애니메이션 분기(정답: 컨페티·확대 효과 / 오답: 흔들림 효과)
5. 전 문항 종료 후 최종 점수 산출 → 랭킹보드 화면 전환

**점수 산정 제안** (창의적 요소, 확정 필요)

| 요소 | 산정 방식 |
|---|---|
| 기본 점수 | 정답 시 100점 |
| 속도 보너스 | 남은 제한시간(초) × 10점 |
| 콤보 보너스 | 연속 정답 시 1콤보당 +10% 가산 |

### 1-3. 랭킹보드 기능

- 게임 종료 시 참가자 점수를 정렬해 표시 (숫자 카운트업 애니메이션)
- 1~3위는 별도 강조 연출(트로피 아이콘, 색상 강조)
- "다시 도전" 버튼으로 재시작
- 첨부된 두 시트(관리자 설정/문제)만으로는 **세션이 끝나면 랭킹 기록이 사라집니다.** 누적 랭킹을 원하시면 3번 항목의 "플레이기록 시트" 추가를 검토해주세요.

### 1-4. UI/UX 유연성 구현 방안

- 선택지 텍스트가 길어지면 자동 두 줄 배치: 고정 높이 대신 `min-height` + `white-space: normal` + flexbox 정렬로 구현, 텍스트 길이에 따라 폰트 크기를 단계적으로 축소(`clamp()` 활용)해 레이아웃 붕괴 방지
- 반응형: 교실 대형 화면(와이드 비율)과 노트북 화면 모두 대응하도록 `vw/vh` 기반 반응형 단위 사용
- 웹캠 인식 신뢰도가 낮을 때 "조명을 밝게 해주세요 / 얼굴이 잘 보이도록 자리를 조정해주세요" 등 실시간 안내 문구 표시
- **접근성 대체 수단(제안)**: 모션 인식이 어려운 환경을 대비해 마우스/터치 클릭으로도 선택 가능한 겸용 모드 제공

### 1-5. 추가 창의적 기능 제안 (선택 적용)

| 기능 | 설명 |
|---|---|
| 정답 해설 카드 | 정답 공개 시 간단한 해설을 함께 노출해 국어 학습 효과 강화 |
| 문제 음성 안내(TTS) | 문제를 음성으로 읽어줘 학생이 화면과 동작에 더 집중 가능 |
| 팀전 모드 | 모둠 단위로 점수를 합산해 모둠 랭킹 표시 |
| 효과음 | 정답/오답/카운트다운 시 사운드 피드백 |

---

## 2. 시스템 아키텍처

```
[브라우저(학생 화면)]                     [Google Apps Script 웹앱]              [Google Sheets]
 index.html/JS                             Code.gs (doGet/doPost)
 ├─ 웹캠 스트림 획득                                                              ├─ 관리자설정 시트
 ├─ 모션 인식(클라이언트 처리) ──fetch(JSON)──▶  요청 라우팅 ───────────────────▶ ├─ 문제 시트
 ├─ 화면 렌더링/애니메이션      ◀──JSON 응답──   시트 CRUD 처리                   └─ (제안) 플레이기록 시트
 └─ 랭킹보드 UI

 배포 위치: GitHub Pages                    배포 위치: GAS 웹앱(exec URL)
```

- **프론트엔드 ↔ 백엔드 통신**: GitHub Pages(정적 호스팅)에서 GAS 웹앱 URL로 `fetch()` 호출. 서로 다른 도메인이므로 CORS 제약을 받음 → 4-3절 통신 규약 참고
- **모션 인식 처리**: 서버 부하 없이 클라이언트(브라우저) WASM 기반으로 실시간 처리 (0-1절 가정 2 참고)

### 2-1. 데이터 로딩 속도 최적화 전략

게임 진행 체감 속도를 높이기 위해 아래 원칙을 전체 구조에 반영합니다.

| 기법 | 적용 방식 | 효과 |
|---|---|---|
| 배치 조회 | `sheet.getDataRange().getValues()`로 **한 번에** 전체 데이터를 읽음 (셀 단위 반복 접근 금지) | Sheets 접근 자체가 병목이므로 배치 조회만으로도 체감 속도가 크게 개선됨 |
| 게임 시작 1회 프리로드 | `getGameData_()` 함수로 설정값 + 랜덤 문제 목록을 **한 번의 요청**에 함께 응답 | 문항마다 서버를 재호출하지 않아 문제 전환이 즉시 이루어짐 |
| 서버측 캐싱 | `CacheService.getScriptCache()`에 문제/설정 데이터를 캐싱(예: TTL 5분), 등록·수정·삭제 시 캐시 즉시 무효화 | 여러 학생이 연속 플레이해도 매번 시트를 다시 읽지 않아 응답 속도 유지 |
| 클라이언트 로컬 진행 | 프리로드된 문제 배열을 브라우저 메모리(JS 변수)에 유지, 문항 전환은 배열 인덱스만 이동 | 네트워크 왕복 없이 즉시 다음 문제 표시 |
| 병렬 초기화 | 데이터 fetch와 동시에 웹캠 권한 요청·모션 인식 모델(WASM) 로딩을 병렬 진행 | 게임 시작 전 대기시간을 데이터 로딩 시간만큼으로 최소화 |
| 페이로드 최소화 | 응답 JSON에 관리자 비밀번호 등 불필요 필드 제외, 필요한 필드만 전송 | 전송량 감소로 로딩 시간 단축 |

---

## 3. 시트구조

### 3-1. 관리자 설정 시트 (`Settings`)

| 열 | 필드명 | 설명 | 예시 |
|---|---|---|---|
| A | key | 설정 항목 키 | `TIME_LIMIT_PER_Q` |
| B | value | 설정 값 | `15` |
| C | description | 설정 설명 | 문항당 제한시간(초) |

주요 설정 키 제안: `TOTAL_QUESTIONS`(출제 문항 수), `TIME_LIMIT_PER_Q`(문항당 제한시간), `MOTION_MODE`(고개기울임/손가락인식), `ANIMATION_ON`(애니메이션 여부), `ADMIN_PASSWORD`(관리자 비밀번호)

### 3-2. 문제 시트 (`Questions`)

| 열 | 필드명 | 설명 |
|---|---|---|
| A | id | 문제 고유 ID |
| B | type | 문제 유형(2지선다/4지선다) |
| C | question | 문제 지문 |
| D~G | option1~4 | 선택지 (2지선다는 D, E만 사용) |
| H | answer | 정답 번호 |
| I | category | 카테고리(문법/어휘/작품이해 등) |
| J | difficulty | 난이도 |
| K | createdAt | 등록일시 |

### 3-3. (제안) 플레이기록 시트 `PlayLog` — 누적 랭킹용

> 요청하신 두 시트(관리자 설정, 문제)에는 포함되어 있지 않은 **선택적 확장안**입니다. 세션이 끝나도 랭킹을 누적해서 보고 싶으실 경우에만 추가를 권장합니다.

| 열 | 필드명 | 설명 |
|---|---|---|
| A | playerName | 참가자명 |
| B | score | 최종 점수 |
| C | correctCount | 정답 수 |
| D | totalCount | 총 문항 수 |
| E | playedAt | 플레이 일시 |

---

## 4. 전체 코드 설계 (파일 구조 및 함수 명세)

> 본 문서는 **계획서**이므로 실제 코드는 포함하지 않았습니다. 아래는 이후 코드 작성 시 따를 파일 구조와 함수 단위 설계입니다.

### 4-1. 프론트엔드 파일 구조 (GitHub 저장소)

```
/
├─ index.html            # 게임 메인 화면
├─ admin.html            # 관리자 설정 화면
├─ css/
│  └─ style.css          # 레이아웃 + 애니메이션(keyframes)
├─ js/
│  ├─ main.js            # 게임 진행 로직(문제 전환, 타이머, 점수)
│  ├─ motionDetector.js  # 모션 인식 전담 모듈(고개 기울임/손가락 인식)
│  ├─ api.js             # GAS 통신 전담 모듈(fetch 래퍼)
│  ├─ ranking.js         # 랭킹보드 렌더링/애니메이션
│  └─ admin.js           # 관리자 화면 로직(문제 CRUD, 설정 저장)
└─ assets/
   ├─ sounds/
   └─ images/
```

### 4-2. `Code.gs` 함수 설계 (모듈화)

| 함수 | 역할 | 비고 |
|---|---|---|
| `doGet(e)` | 조회용 라우터 (`?action=getQuestions`, `?action=getSettings`) | 읽기 전용, 인증 불요 |
| `doPost(e)` | 쓰기용 라우터 (`action` 파라미터로 분기) | 관리자 액션은 비밀번호 검증 필수 |
| `getQuestions_()` | 문제 시트 전체를 `getValues()` **1회 배치 조회**로 가져옴(셀 단위 반복 조회 금지) | — |
| `getGameData_()` | **설정값 + 랜덤 추출 문제 목록을 한 번에 반환**하는 게임 시작 전용 함수. 내부적으로 `getQuestions_()` 결과를 `CacheService`에 캐싱해 재사용 | 게임 시작 시 1회 호출 — 빠른 로딩의 핵심(2-1절) |
| `addQuestion_(data)` | 문제 등록, **입력값 검증**(빈 값/타입 체크) 후 시트 append, 등록 후 캐시 무효화 | 관리자 전용 |
| `updateQuestion_(id, data)` | 문제 수정 | 관리자 전용 |
| `deleteQuestion_(id)` | 문제 삭제 | 관리자 전용 |
| `getSettings_()` | 관리자 설정 전체 조회 | — |
| `updateSettings_(data)` | 설정 저장 | 관리자 전용, 비밀번호 필드는 응답에서 제외 |
| `verifyAdmin_(password)` | 관리자 비밀번호 대조 | 모든 관리자 액션 전에 호출 |
| `logPlayResult_(data)` | (제안) 플레이 결과 기록 | `PlayLog` 시트 사용 시 |

- **보안/유지보수 원칙**: 관리자 전용 함수는 반드시 `verifyAdmin_()`을 거치도록 하고, 모든 쓰기 함수는 입력값 존재 여부·타입을 검증한 뒤 처리 (권한 분리 + 입력 검증)
- 각 함수에는 역할/파라미터/반환값을 설명하는 주석을 포함해 유지보수 용이성 확보

### 4-3. API 통신 규약

| 구분 | 메서드 | 이유 |
|---|---|---|
| **게임 시작(데이터 프리로드)** | GET `?action=getGameData` | **설정 + 문제 목록을 한 번의 요청으로 함께 응답** → 이후 문항 전환 시 추가 호출 없이 클라이언트 로컬 배열에서 진행 (2-1절) |
| 조회(문제/설정 불러오기) | GET | `fetch(url + '?action=...')` — 단순 요청으로 CORS 사전요청(preflight) 없음 |
| 등록/수정/삭제/설정저장 | POST, **`Content-Type` 헤더 미설정**(브라우저 기본값 `text/plain`) | GAS 웹앱은 커스텀 헤더 preflight(OPTIONS)에 응답하지 않아 `application/json` 헤더를 명시하면 CORS 오류 발생. Body는 JSON 문자열로 보내고 서버에서 `JSON.parse(e.postData.contents)`로 파싱 |

응답 포맷 예시(공통):
```json
{ "success": true, "data": { ... } }
{ "success": false, "error": "메시지" }
```

---

## 5. 모션 인식 기술 스택

| 항목 | 내용 |
|---|---|
| 라이브러리 | MediaPipe **Tasks Vision** (`FaceLandmarker`, 필요 시 `HandLandmarker`/`GestureRecognizer`) — 브라우저에서 WASM으로 동작, 서버 전송 없이 클라이언트에서 실시간 추론 |
| 고개 기울임 판정 | 얼굴 랜드마크 중 양쪽 눈 좌표로 기울기(roll angle)를 `atan2`로 계산, 임계 각도 초과 시 방향 판정 |
| 손가락 개수 판정 | 손 랜드마크의 각 손가락 마디 굽힘 여부로 펼쳐진 손가락 수 카운트 |
| 실행 환경 요건 | HTTPS 필수(웹캠 `getUserMedia` 권한은 HTTPS에서만 정상 동작) — GitHub Pages는 기본 HTTPS 제공이므로 요건 충족 |

---

## 6. 배포 방법

1. **Google Sheets 준비**: 새 스프레드시트 생성 → `Settings`, `Questions` (필요 시 `PlayLog`) 시트 생성 및 헤더 입력
2. **Apps Script 작성**: 스프레드시트 `확장 프로그램 > Apps Script`에서 `Code.gs` 작성
3. **웹앱 배포**: `배포 > 새 배포 > 웹 앱` 선택 → 실행 계정: 본인, 액세스 권한: **모든 사용자** → 배포 후 발급되는 `exec` URL 확보
4. **GitHub 저장소 준비**: 저장소 생성 후 4-1절 구조로 프론트엔드 코드 업로드(push)
5. **GitHub Pages 활성화**: 저장소 `Settings > Pages`에서 배포 브랜치 지정
6. **연동**: `js/api.js`의 GAS 웹앱 URL 상수에 3단계에서 발급받은 `exec` URL 입력
7. **테스트**: 관리자 화면에서 문제 등록 → 시트 반영 확인 → 게임 시작 → 모션 인식/타이머/랭킹보드 동작 확인 (웹캠 권한 허용 필요)
8. **재배포 시 주의**: `Code.gs` 수정 후에는 반드시 `배포 > 배포 관리 > 새 버전`으로 재배포해야 변경사항이 실제 웹앱에 반영됨

---

## 7. 개발 로드맵 (단계별 체크리스트)

- [ ] 1단계: 시트 구조 확정 및 생성 (3절)
- [ ] 2단계: `Code.gs` 기본 CRUD 함수 구현(배치 조회·캐싱 포함) 및 배포 테스트 (4-2, 2-1절)
- [ ] 3단계: 관리자 화면(`admin.html`) — 문제 등록/설정 기능 구현
- [ ] 4단계: 게임 메인 화면 — 문제 출제/타이머/UI 골격 구현 (모션 인식 제외, 클릭 방식으로 우선 검증)
- [ ] 5단계: 모션 인식 모듈(`motionDetector.js`) 통합 (5절)
- [ ] 6단계: 애니메이션/사운드 등 UI 완성도 작업 (1-4, 1-5절)
- [ ] 7단계: 랭킹보드 구현 및 점수 로직 확정
- [ ] 8단계: 교실 환경(웹캠 각도, 조명) 실기 테스트 및 임계값 조정
- [ ] 9단계: 최종 배포 및 실사용 리허설

---

## 8. 보안 및 유지보수 고려사항

- 관리자 기능은 비밀번호 검증(`verifyAdmin_`)을 통과한 요청만 처리 (권한 분리)
- 모든 쓰기 요청은 서버 단에서 입력값 검증(빈 값, 타입, 길이 제한) 수행 — 클라이언트 검증만으로는 불충분
- `PropertiesService`를 활용해 민감 값(관리자 비밀번호 등)을 시트 대신 스크립트 속성에 저장하는 방안도 추후 검토 가능
- 함수 단위 분리 및 주석 작성으로 유지보수성 확보 (4-2절 구조 준수)
- 신속한 데이터 로딩 요구사항 반영: `Questions`/`Settings` 조회 결과를 `CacheService`로 캐싱(TTL 예: 5분)하고, 문제 등록·수정·삭제 시 해당 캐시를 즉시 무효화해 최신성과 속도를 동시에 확보 (2-1절)
